     <div class="container-fluid">
  
<%if product%>    
   
	<strong><%=product.name %> - <%= link_to 'Goto Product', product_path(product) , class:"btn btn-success"%> </strong>
	</p>
	<%= link_to 'Edit', edit_product_path(product) , class:"btn btn-success"%> 
	<%if current_admin.companyadmin?%>
	<%= link_to "delete", [product.station, product], method: :delete, data: {confirm: "Are you sure you want to delete this product"}, class:"btn btn-danger"%><%end%>
	 <p><%= time_ago_in_words(product.created_at)%> ago	
     
	 <%end%>
	</div>
	

	
